const SMSClient = require('./sms');
const ImageStorageClient = require('./image-storage');
const DistanceMatrixClient = require('./distance-matrix');

module.exports = {
	SMSClient,
	ImageStorageClient,
	DistanceMatrixClient
};
